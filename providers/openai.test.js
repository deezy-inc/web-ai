/* eslint-env jest */

const { openai } = require('../lib/web-ai.cjs');

describe('dummy test', () => {
    test('try out cjs file', async () => {
        /*
        const resp = await openai.createChatCompletion({
            model: 'gpt-3.5-turbo',
            messages: [{
                "role": "system",
                "content": "tell me a joke"
            }]
        })
        console.log(resp)
        console.log(resp.choices[0].message)
        */
    });
});
